export const LANG_COOKIE_DURATION = 60 * 60 * 24 * 15; // 15 days

export const LANG_COOKIE_NAME = "LOCALE";

export const support_langs = ["en", "fa"];

export const INFO_LANGS: { [key: string]: { dir: string, label: string } } = {
  en: { dir: "ltr", label: "English" },
  fa: { dir: "rtl", label: "فارسی" },
};

